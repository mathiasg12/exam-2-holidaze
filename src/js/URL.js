const baseURL = 'https://v2.api.noroff.dev';
const allVenuesURL = `${baseURL}/holidaze/venues`;
const registerURL = `${baseURL}/auth/register`;
const LoginURL = `${baseURL}/auth/login`;
const ApiKeyURL = `${baseURL}/auth/create-api-key`;
const bookingsURL = `${baseURL}/holidaze/bookings/`;
const profileURL = `${baseURL}/holidaze/profiles/`;
export {
  allVenuesURL,
  registerURL,
  LoginURL,
  ApiKeyURL,
  bookingsURL,
  profileURL,
};
